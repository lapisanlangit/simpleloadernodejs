# Simple Loader Node JS

	Ini adalah project dasar pembuatan loader dengan menggunakan node-cron node JS
	
## Instalasi
	
	jalankan `npm install` pada folder project`
	untuk menjalankannya : `node index.js`
	

